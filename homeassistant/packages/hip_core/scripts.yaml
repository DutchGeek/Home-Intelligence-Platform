script:
  hip_log_event:
    alias: HIP Log Event
    mode: queued
    fields:
      message:
        required: true
    sequence:
      - action: logbook.log
        data:
          name: HIP
          message: "{{ message }}"
